interface SectionTitleProps {
    img: string;
    name: string;
}

export const SectionTitle = ({img, name}: SectionTitleProps) => (

    <div className="flex flex-row justify-center">

    </div>

)